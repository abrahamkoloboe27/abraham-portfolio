# URL publique de l'API FastAPI (sans slash final)
NEXT_PUBLIC_API_URL=http://localhost:8000

# URL publique du site, utilisée pour les URLs canoniques, l'OG et le sitemap
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Durée (en secondes) avant revalidation des pages en arrière-plan
NEXT_PUBLIC_REVALIDATE=300
